# Simulated Microsoft Graph responses

These fixtures contain synthetic identifiers and reserved `.invalid` hostnames.
They are used only by unit tests; no test in this directory may contact a real
Microsoft Graph, Microsoft 365, or SharePoint endpoint.
